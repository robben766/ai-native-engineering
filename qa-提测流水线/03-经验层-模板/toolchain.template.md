# 工具适配层（Toolchain Adapter）— 模板

> 复制为目标项目的 `docs/experience/toolchain.md`。声明本项目用什么工具，引擎按此绑定。
> 填完这一页，引擎里所有"调工作项系统/代码平台/路由索引"的抽象动作就有了具体落点。

## 工作项系统（Issue Tracker）
- 系统：`TAPD / Jira / 禅道 / GitHub Issues / ____`
- 接入方式：`MCP 工具名 / REST API`
- 关键操作映射：
  - 查工作项：`____`
  - 发评论：`____`
  - 写用例：`____`（无用例库则改为评论附用例）
  - 关联工作项与用例：`____`
- 号码识别规则：`____`（如纯数字；或 PROJ-123）

## 代码平台（VCS / MR）
- 平台：`GitLab / GitHub / Gitee / ____`
- 接入方式：`MCP 工具名 / git 本地`
- 关键操作映射：
  - 取 MR/PR 变更：`____`
  - 分支对比：`____`（如 `compare` / 本地 `git diff base...head`）
- 主分支约定（各仓）：`____`

## 路由索引（可选，用于影响面反查）
- 是否有：`有 / 无`
- 位置：`____`
- 生成方式：`____`
- 无索引时降级：影响面反查改为"读被改接口的调用方代码 + 领域文档"

## 判仓规则（多仓项目才需要）
- 是否多仓：`是 / 否`
- 判定依据：`____`（如"控制器文件路径 + API 路由前缀"双证据）
- 已知同名歧义：`____`

## 浏览器自动化（E2E）
- 工具：`Playwright MCP / ____`
- 登录方式：`账号密码 / SSO / 验证码(需人工)`
- 环境访问：`会话所在机器能否直连测试环境（内网/VPN）`

## 业务领域文档（可选）
- 是否有：`有 / 无`
- 位置：`____`
- 用途：用例生成/影响面分析时提供业务上下文

## 部署（阶段 6）
- 首期：`人工闸口（QA 部署后告知就绪）`
- 未来：`运维平台 MCP —— 工具名/触发方式/就绪探测：____`
